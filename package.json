{
  "name": "queens-calendar",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "ingest": "node scripts/ingest-events.mjs",
    "check:pipeline": "node scripts/check-event-pipeline.mjs",
		"check:integration": "node scripts/check-built-site.mjs",
		"check:release": "npm run check:pipeline && npm run check && npm run build && npm run check:integration",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/plus-jakarta-sans": "^5.2.8",
    "@fontsource/be-vietnam-pro": "^5.2.8",
    "astro": "^6.1.7",
    "cheerio": "^1.2.0",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@types/leaflet": "^1.9.21",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  }
}
